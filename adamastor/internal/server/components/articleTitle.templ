
package components

import "strings"

templ ArticleTitle(title string, endpoint templ.SafeURL) {
	<div class="article-title-section article-section">
		<h2><a class="hack-text" data-value={ strings.ToUpper(title) } href={ endpoint }>{ strings.ToUpper(title) }</a></h2>
	</div>
}
